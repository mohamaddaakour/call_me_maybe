[
  {
    "name": "fn_book_flight",
    "description": "Book airline seats from an origin city to a destination city.",
    "parameters": {
      "origin": {"type": "string"},
      "destination": {"type": "string"},
      "passengers": {"type": "integer"}
    },
    "returns": {"type": "string"}
  },
  {
    "name": "fn_set_alarm",
    "description": "Set an alarm at a requested time and optionally repeat it daily.",
    "parameters": {
      "time": {"type": "string"},
      "repeat_daily": {"type": "boolean"}
    },
    "returns": {"type": "boolean"}
  },
  {
    "name": "fn_convert_temperature",
    "description": "Convert a temperature value to a requested temperature scale.",
    "parameters": {
      "value": {"type": "number"},
      "target_scale": {"type": "string"}
    },
    "returns": {"type": "number"}
  },
  {
    "name": "fn_lookup_product",
    "description": "Look up a product in the catalog using its SKU identifier.",
    "parameters": {
      "sku": {"type": "string"}
    },
    "returns": {"type": "string"}
  }
]
